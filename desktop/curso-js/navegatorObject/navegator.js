//appCodeName = return The name of the browser (Your answer is not always correct)
// appName = return name (Your answer is not always correct)
// appVersion = return the version  (Your answer is not always correct)
// connection =  return all data
//geolocation = return geolocation object
//hardwareConcurrency =  return PC nuclei
// language = return el languages
//mimeTypes= return the type header
// online =return If it's online
// useAgent = return user information (Your answer is not always correct)
//cookieEnable =  return if is enable the cookies
//permission = Allows access to each of the browser permits
// platform = return  the Windows, Mac or Linux operating system
//plugins =  return The plugins installed
// product = return Gecko
/*   memorization */

console.log(navigator.hardwareConcurrency)
